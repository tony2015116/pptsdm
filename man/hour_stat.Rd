% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hour_stat.R
\name{hour_stat}
\alias{hour_stat}
\title{Hourly Statistics Computation}
\usage{
hour_stat(data, target_date = Sys.Date() - 1)
}
\arguments{
\item{data}{data.table, required.
The input data table which must contain the following columns:
\code{visit_time} (POSIXct), \code{location} (character), \code{duration} (numeric, in minutes),
and \code{feed_intake} (numeric, in grams).}

\item{target_date}{Date, optional.
The specific date for which the data should be filtered. Defaults to 35 days prior
to today (using \code{Sys.Date() - 35}).}
}
\value{
A list of data.tables containing hourly statistics:
\itemize{
\item \code{visit_n}: Number of visits per location per hour.
\item \code{feed_time}: Total feed time in hours per location per hour.
\item \code{feed_intake}: Total feed intake in kilograms per location per hour.
}
}
\description{
The \code{hour_stat} function computes hourly statistics for given data,
including the number of visits, total feed time, and total feed intake,
grouped by location and hour. This function is designed for processing
time-series data related to feeding or visit behaviors.
}
\note{
The function requires that the \code{data} table contains specific columns named
\code{visit_time}, \code{location}, \code{duration}, and \code{feed_intake}. The \code{visit_time} should
be in POSIXct format for accurate processing.
}
\examples{
# Load CSV data
data <- data.table::fread("C:/Users/Dell/Documents/projects/pptsdm_data/ppt_monitor_test_data.csv")
print(hour_stat(data = data))
}
